/**
 * API Common - Compatibility Layer
 * Re-exports from new lib/api/common.ts for backward compatibility
 */

export { 
  apiClient, 
  API_BASE_URL, 
  buildQueryString,
  type ApiClientConfig,
  type RequestOptions
} from '../lib/api/common';